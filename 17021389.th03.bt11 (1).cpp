#include <iostream>
#include <vector>
#include <iomanip>

using namespace std;

int main()
{
    vector<long> a; //s?ssa

    long x, count = 0;
    float sum = 0, u =
    {
        a.push_back(x);

        /*if(x < -100)
        {
            break;
        }*/
        sum += x;
        count++;


    }
    sum /= count;
    long i;
    long taget;
    for(i = 0 ; i < count; i++)
    {

        u += (a[i] - sum) * (a[i] - sum);

    }
    u /= (count );

    cout << fixed << setprecision(2) << sum << "\n" << u;
// hdshdksd
}
